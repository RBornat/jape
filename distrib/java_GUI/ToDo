Bugs (engine): not keeping the picture steady on undo

Bugs (interface): what's with the 'next selection on same line'?

----------------

text field in new conjecture dialog should be text area

pane focus is invisible

Cancel in choose file dialog after Save/Cancel/Don'tSave shouldn't mean Don'tSave

panel maximise button

Localise file dialogs

PanelWindow needs to use minwaste to pack buttons

Protocol in:

DROPBEGIN
DROPEND
HOWTO
LOADFONT
POINTSIZE
PROCRUSTES
READHIGHLIGHT
SETALERT
SETCOMMENT
SHOWFILE
SHOWPROVISOLINE
TERMINATE

Protocol out:

DRAGQ
DROP

Unused commands (many are designed to be issued by hand from Text Command window, so no big deal):

"apply", comm
"assign", name :: value
"refreshdisplay", [] (ought to be dead)
"steps", quota
"steps", []
"tellinterface", name :: interfacecommand
"version", []
"saveengine", name (!!)
"print", [path]
"quitnow", []
"dragquery", []  (moribund)
"dropcommand", [] (moribund)
"hideroot", []
"exposeparent", []
"hidecut", []
"profile", ["on"]
"profile", ["off"]
"profile", ["reset"]
"profile", ["report"]
"profile", ["report"; filename]
"showfile", [filename]
"createdbugfile", []
"closedbugfile", []